import Foundation

// Este archivo es solo un ejemplo. No se usa en la compilación real.
// Para configurar tu entorno local, duplica este archivo como "Secrets.swift" y cambia la URL.
// Cambiamos el nombre de la estructura aquí a "SecretsExample" para que no choque con tu Secrets real.
struct SecretsExample {
    // Ejemplo de configuración para desarrollo.
    // Los desarrolladores deben duplicar este archivo y renombrarlo a "Secrets.swift"
    static let apiBaseURL = "http://localhost:3000/api"
}
